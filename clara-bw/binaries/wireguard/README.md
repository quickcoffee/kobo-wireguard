# WireGuard binaries

Place ARMv7 Linux binaries in this folder before running the installer:

- `wireguard-go`
- `wg` (from wireguard-tools)

They are copied to `/mnt/onboard/wireguard/bin/` and symlinked into `/usr/bin/`.
